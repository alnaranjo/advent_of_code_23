{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "./build.sh",
      "args": ["${input:build_target}"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ],
  "inputs": [
    {
      "id": "build_target",
      "description": "Build target",
      "default": "",
      "type": "promptString"
    }
  ]
}
